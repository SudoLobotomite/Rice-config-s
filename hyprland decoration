-----------------------
---- LOOK AND FEEL ----
-----------------------

-- Refer to https://wiki.hypr.land/Configuring/Basics/Variables/

hl.config({
    general = {
        gaps_in = 5,
        gaps_out = 10,
        border_size = 2,

        "col.active_border = rgba(ab8359ff)",
        "col.inactive_border = rgba(ab8359ff)",
    },

    decoration = {
        rounding = 10,
        rounding_power = 2,

        active_opacity = 1.0,
        inactive_opacity = 1.0,

        shadow = {
            enabled = true,
            range = 4,
            render_power = 3,
            color = 0xee1a1a1a,
        },

        blur = {
            enabled = false,
            size = 8,
            passes = 3,
            new_optimizations = true,
        },
    },

    animations = {
        enabled = true,
    },
})
